# Copy this file to `.env` and fill in your local values.
#
# Backend reads only DATABASE_URL. The React frontend has its own env file
# at `frontend/.env` (see `frontend/.env.example`).

DATABASE_URL=postgresql+psycopg2://postgres:postgres@127.0.0.1:5432/smartattend
